name: ML Model CI Workflow

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main

jobs:
  train:
    runs-on: ubuntu-latest

    steps:
      # 🔹 Step 1: Checkout repository
      - name: 🔹 Checkout repository
        uses: actions/checkout@v3

      # 🔹 Step 2: Setup Miniconda environment
      - name: 🔹 Setup Miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          activate-environment: student_performance_env
          environment-file: MLProject/conda.yaml
          python-version: 3.9
          auto-activate-base: false
          use-mamba: true

      # 🔹 Step 3: Verify environment setup
      - name: 🔹 Verify Conda Environment
        shell: bash -l {0}
        run: |
          conda info
          conda list

      # 🚀 Step 4: Run MLflow Project (training model)
      - name: 🚀 Run MLflow Project
        shell: bash -l {0} # penting agar conda aktif di shell login
        run: |
          cd MLProject
          echo "🔧 Menginisialisasi environment MLflow..."
          conda init bash
          source ~/.bashrc
          conda activate student_performance_env
          echo "🚀 Menjalankan MLflow project..."
          mlflow run . -P data_path=siswa_clean.csv
